/// Represents the view modes for the detector.
enum DetectorViewMode {
  /// Indicates that the detector is using a live feed for detection.
  liveFeed,

  /// Indicates that the detector is using a gallery of images for detection.
  gallery,
}
